using UnityEngine;

public class FloatingTextSpawner : MonoBehaviour
{
    public static void Spawn(string text, Vector3 pos, Color color)
    {
        Debug.Log(text); // tạm thời

        // nếu có prefab TextMeshPro thì instantiate ở đây
    }
}